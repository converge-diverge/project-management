<!doctype html>
<html>
  <head>
    <title>Thank You</title>
  </head>
  <body>
    <h1>Thank you for your response!</h1>

    {{#each projects}}
      <div>
      {{site}}: {{project}}
      {{#if status.hasConsented}}
        will be included in this Opt-In Analysis.
      {{else}}
        will not be included in this Opt-In Analysis.
      {{/if}}
      </div>
    {{/each}}

    <p>
    Your response can be modified <a href="/consent/form/{{personID}}">here</a>.
  </body>
</html>